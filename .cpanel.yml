deployment:
  tasks:
    - export WEBROOT=/home/adsaccel/public_html
    - /bin/mkdir -p $WEBROOT/tools
    - /usr/bin/rsync -av --ignore-existing --exclude='.git' tools/ $WEBROOT/tools/
    - /usr/bin/rsync -av tools.json $WEBROOT/
